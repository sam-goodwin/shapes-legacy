import 'jest';

import * as gql from '../lib';

it('should', () => {
  // const query = client.compileQuery({id: gql.ID}, ({id}, root) => {
  //   console.log(id, root);
  //   return root.getPerson({id}, person => person
  //     .id()
  //     .name()
  //     .$on('Dog', dog => dog
  //       .bark())
  //     .$on('Bird', bird => bird
  //       .tweet())
  //   );
  // });
  // expect(query.operationDefinitionNode).toEqual({
  //   a: ''
  // });
});