{
  "name": "@shapes/jsonschema",
  "description": "Adds support for JSON Schema generation adn validation to Shapes",
  "version": "0.1.0",
  "main": "dist/cjs/index.js",
  "types": "dist/cjs/index.d.ts",
  "module": "dist/esm/index.js",
  "scripts": {
    "clean": "rm -rf dist"
  },
  "license": "Apache-2.0",
  "author": {
    "name": "Sam Goodwin",
    "email": "sam@shapes.org"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/punchcard/shapes"
  },
  "readme": "README.md",
  "keywords": [
    "shapes"
  ],
  "devDependencies": {
    "@shapes/core": "^0.1.0"
  },
  "peerDependencies": {
    "@shapes/core": "^0.1.0"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "/node_modules/"
    ],
    "testMatch": [
      "**/?(*.)+(spec|test).ts?(x)"
    ],
    "preset": "ts-jest",
    "globals": {
      "ts-jest": {
        "tsconfig": "tsconfig.json"
      }
    }
  }
}
